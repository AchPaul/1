# 🌱 Инструкция по настройке PWA GrowHub

## 📱 Как подключиться к своей теплице

### Вариант 1: Первый запуск (рекомендуется)

1. **Откройте PWA в браузере:**
   ```
   https://<username>.github.io/growhub/
   ```

2. **При первом запуске** появится форма "⚙️ Настройка подключения"

3. **Заполните поля:**
   - **MQTT Host:** адрес вашего брокера
   - **WebSocket Port:** TLS
   - **MQTT Username:** ваш логин
   - **MQTT Password:** ваш пароль
   - **Base Topic:** ваш-mac

4. **Нажмите "Сохранить"**

5. **Готово!** Данные сохранены в браузере. При следующем открытии PWA подключится автоматически.

---

### Вариант 2: Быстрая настройка через URL

Можно передать параметры через URL (кроме пароля):

```
https://<username>.github.io/growhub/?h=broker.hivemq.cloud&p=8884&u=myuser&b=aabbccdd
```

При открытии:
1. Форма настройки откроется автоматически
2. Поля будут предзаполнены
3. Вам нужно ввести только пароль
4. Нажать "Сохранить"

**⚠️ ВАЖНО:** Не передавайте пароль в URL! Это небезопасно.

---

## 🔧 Управление настройками

### Изменить настройки
1. Нажмите "⚙️ Настройка подключения" на главной странице
2. Измените нужные поля
3. Нажмите "Сохранить"

### Очистить настройки
1. Откройте форму настройки
2. Нажмите кнопку "Очистить"
3. Все данные будут удалены из браузера

---

## 🏠 Настройка ESP32 (прошивка)

### В локальном веб-интерфейсе ESP32:

1. Подключитесь к ESP32 по адресу: `http://192.168.x.x`
2. Откройте "Сервисные настройки" → MQTT
3. Введите данные:
   ```
   Host: broker.hivemq.cloud
   Port: 8884
   User: myuser
   Password: mypassword
   ```
4. Сохраните и перезагрузите ESP32

---

## ☁️ Настройка облачного MQTT брокера

### HiveMQ Cloud (бесплатно)

1. Зарегистрируйтесь на https://www.hivemq.com/mqtt-cloud-broker/
2. Создайте кластер
3. Получите данные:
   ```
   Host: <cluster>.hivemq.cloud
   Port: 8883 (MQTT) / 8884 (WebSocket)
   Username: <ваш логин>
   Password: <ваш пароль>
   ```
4. Введите эти данные в ESP32 и PWA

---

## 🔐 Безопасность

✅ **Что безопасно:**
- Данные хранятся в localStorage вашего браузера
- Никто кроме вас не имеет доступа к паролю
- Используйте TLS порт (8884/8883)

❌ **Что НЕ безопасно:**
- Передавать пароль через URL
- Использовать незащищенный порт (1883/9001) через интернет
- Делиться ссылкой с параметрами подключения

---

## 📊 Статус подключения

**Статусы в PWA:**
- `Подключение...` — идет соединение с MQTT
- `Подключено` — всё работает
- `Отключено` — потеряно соединение
- `Введите настройки подключения` — нужно настроить

---

## ❓ Частые вопросы

**Q: PWA не подключается к MQTT**  
A: Проверьте:
- Правильность host, port, user, pass
- ESP32 подключен к тому же брокеру
- WebSocket порт открыт на брокере (обычно 8884 для TLS)

**Q: Данные не обновляются**  
A: 
- Проверьте Base Topic (должен совпадать с ESP32)
- ESP32 должен публиковать в `<mac>/state/json`

**Q: Как узнать Base Topic?**  
A: В локальном веб-интерфейсе ESP32 или в серийном мониторе при старте

**Q: Можно ли использовать без интернета?**  
A: Да, если используете локальный MQTT брокер в вашей сети

---

## 🚀 Деплой PWA на GitHub Pages

1. Создайте репозиторий на GitHub (публичный)
2. Загрузите содержимое `remote_ui/` в корень
3. Settings → Pages → Source: `main` branch
4. Получите URL: `https://<username>.github.io/<repo>/`

---

**Готово!** Теперь вы можете управлять теплицей из любой точки мира! 🌍
