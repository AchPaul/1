# Инструкция по отладке алертов в GrowHub Remote UI

## Проблема
Алерты датчиков отображаются всегда, даже когда ESP32 отправляет значения 0.

## Шаги для диагностики

### 1. Очистите кэш браузера
Так как используется Service Worker, старая версия может быть закэширована:
1. Откройте DevTools (F12)
2. Перейдите на вкладку **Application** (или **Приложение**)
3. В разделе **Service Workers** нажмите **Unregister**
4. Очистите кэш: **Clear storage** → **Clear site data**
5. Перезагрузите страницу (Ctrl+Shift+R)

### 2. Проверьте консоль браузера
После обновления страницы откройте консоль (F12 → Console):

Вы должны увидеть:
```
📦 Received JSON: {name: "GrowCap", err_sensor_temp: 0, err_sensor_hg: 0, err_sensor_dht: 0, ...}
🚨 Alerts state: {
  alert_water: false,
  alert_humid: false,
  alert_high_temp: false,
  alert_low_temp: false,
  err_sensor_temp: false,  ← должно быть false для значения 0
  err_sensor_hg: false,
  err_sensor_dht: false
}
🔍 Processing alerts...
  alert_water: isActive=false, display will be: none
  alert_humid: isActive=false, display will be: none
  ...
  err_sensor_temp: isActive=false, display will be: none  ← должно быть none
  err_sensor_hg: isActive=false, display will be: none
  err_sensor_dht: isActive=false, display will be: none
✅ Total active alerts: false, section display: none
```

### 3. Проверьте MQTT сообщение
Убедитесь, что ESP32 отправляет правильный формат:
```json
{
  "err_sensor_temp": 0,
  "err_sensor_hg": 0,
  "err_sensor_dht": 0
}
```

**НЕ должно быть:**
- Строк: `"err_sensor_temp": "0"` или `"err_sensor_temp": "false"`
- Отсутствующих полей (если поле не отправлено, оно будет `undefined` → false)

### 4. Проверьте CSS
Откройте DevTools → Elements и найдите элементы с классом `.alert`.
Проверьте computed style:
- `display` должен быть `none` для неактивных алертов
- `display` должен быть `flex` для активных алертов

### 5. Тестовая страница
Откройте `test-alerts.html` в браузере для проверки:
- Все алерты должны быть скрыты по умолчанию
- При нажатии на кнопку соответствующий алерт появляется
- Секция алертов скрывается, когда нет активных

## Возможные причины проблемы

### A. Кэширование старой версии
**Решение:** Очистите Service Worker и кэш (см. шаг 1)

### B. ESP32 отправляет неправильный формат
**Проверьте в mqtt.cpp:**
```cpp
js += F("\"err_sensor_temp\":"); js += (err_temp_flag?1:0); js += F(",");
```
Должно генерировать число 0 или 1, не строку.

### C. Проблема с парсингом JSON
**Проверьте консоль:** Если есть ошибка `State parse error`, JSON невалидный.

### D. CSS конфликт
Проверьте, что нет других стилей, переопределяющих `display`.

### E. JavaScript ошибка до обработки алертов
Если код падает с ошибкой до строки обработки алертов, они останутся в дефолтном состоянии.

## Исправление

После обновления `app.js` с debug-логами:
1. Очистите Service Worker
2. Перезагрузите страницу
3. Проверьте логи в консоли
4. Если `isActive=false` но алерты видны → проблема в CSS/HTML
5. Если `isActive=true` хотя ESP32 шлёт 0 → проблема в парсинге/формате данных

## Отключение debug-логов
После решения проблемы закомментируйте `console.log` в `app.js`:
```javascript
// console.log('📦 Received JSON:', js);
// console.log('🚨 Alerts state:', ...);
```

И обновите версию кэша в `service-worker.js`:
```javascript
const CACHE = 'gh-remote-v6'; // увеличьте версию
```
